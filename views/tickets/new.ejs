<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>New Flight Ticket</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <h1>New Flight Ticket</h1>
    <%- include('../partials/nav') %>
    <form action="/flights/<%= flight._id %>/tickets" method="POST">
        <select name="seat">
            <% for(seat of allSeats) { %>
                <% if (!sold.includes(seat)) { %>
                    <option><%= seat %></option>
            <% }} %>
        </select>
        <input type="number" name="price" value="0" placeholder="Price in USD">
        <button type="submit" class="btnAdd">Purchase</button>
    </form>
    <hr>
    <form action="/flights/<%= flight._id %>">
        <button class="btnCancel">Cancel</button>
    </form>
</body>

</html>